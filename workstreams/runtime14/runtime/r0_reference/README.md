# R0 reference boundary

The complete R0 extract is an ignored, hash-verified input, not a public asset
distribution. Its entire path/size/hash inventory is in `contracts/lineage.json`.
Composition preserves it unchanged except for the exact twelve-file pure-R1 overlay.

`accepted_tiefling_bootstrap.lua` is the exact project-owned accepted provider script
used to execute historical API1/API2 controls. It is not an R0 Runtime override and
is never included by the composer. It retains its original hash and warning.
