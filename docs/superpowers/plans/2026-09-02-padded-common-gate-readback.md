# Padded common-gate frozen-candidate readback plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development with strict TDD and independent review. This is retrospective evidence reconciliation, not a new geometry method or an exclusion approval.

**Goal:** Evaluate the retained Padded candidate artifacts under one explicitly recorded current readback profile and expose every remaining architecture/component/policy gap before considering a terminal disposition.

**Architecture:** Preserve the accepted current run and every historical artifact. Define a new immutable retrospective evaluation profile; verify the original source/body and exact frozen candidate hashes; parse and evaluate each candidate through the existing geometry/readback functions without running its generator. Keep original construction gates, current comparison gates, and terminal-policy eligibility distinct.

**Authorities:** `docs/superpowers/specs/2026-09-01-clothmorph-terminal-exclusion-policy.md`, especially 4.2; `.superpowers/reviews/padded-final-method-policy-review.md` (SHA `7168852A4D411E947BCDF64F7A86B0B4225C76EC3FDBF5A471DF282B912245F8`); current source frozen at a99446e and documentation checkpoint 6b1b4cbbec4114b7e76ad3c094626df49d64e242.

## Fixed evidence and restrictions

- Exact source DAE SHA `DB6C143EE853385BA5A4FDEE9CF60E85030203856596256C37EE8A0D45AAA262`; body GLB SHA `51D4D723EB945CD16E0EF99296CFBD8050013D6FA74D863C5A7ACF962746328C`.
- Accepted current repeated search SHA `5DAD7E910EB0E09E9E53EC9EE9444F916A9669AF885C6F7AD182432C98B9CA0A`: zero passing among the fixed 160 cases, after certified targets. Do not rerun or alter it.
- Parent corpus: `ChatGPT Work Files/Padded_BGWatch_TopologyRecovery_20260826`, exact ARTIFACT_MANIFEST and source/control manifest pinned by the existing workstream.
- Frozen transfer candidate SHA `A3AAA64FFFD8E83C00ADAA0BD5870155159DEFF519D973CE8253BDB0BBA7245E`.
- Frozen post-clip candidate SHA `ADBBD323D587AC2D9A1C3069F73BB86505A440822BAD64E319A938727EEC9015`; locate only through verified source/control evidence.
- Frozen alpha candidate SHA `99F11C2E55BA1307C0EFC0860121766DECFCEEA3CC9B75C1665A9AA38BF5ED78`.
- Frozen local candidate SHA `DF0DF226697BF22E5A5F646F4BF85FBF77C7FAD56E110BF9B75FD3C38FA7280B`.
- No source, protected route, original evidence, geometry, generator, live game, save/profile, GR2 or PAK change. Do not change the completed search/writer semantics or invent canonical ledger/event identifiers.
- Missing silhouette/nontriviality predeclaration, complete component binding, generator repeatability, or source-profile identity stays explicitly unresolved. Do not invent thresholds after observing failures.
- The old local repair is `TARGET_NOT_CERTIFIED`; evaluating its frozen output does not repair that prerequisite or qualify it as a correctly constructed exhausted target method.

### Task 1: Implement an immutable retrospective readback profile and evaluator

**Files:** new `workstreams/padded_bgwatch_closure/retrospective.py`, `tests/test_retrospective.py`, and a versioned profile contract under `contracts/`. Existing production modules remain unchanged unless a minimal export is essential and independently tested.

- [ ] Freeze the profile before actual comparative evaluation. Label it `RETROSPECTIVE_FROZEN_ARTIFACT_READBACK`; state that its gates did not govern historical construction.
- [ ] Bind exact source/body/geometry identities, original active IDs, fixed cohort, oracle, six-decimal serialization, published area [0.5,2.0], current internal area [0.51,1.99], cosine 0.05, zero flips/new zeros, positive 0.001 clearance with ambiguity rejection, coverage projection/distance 0.05, and non-POSITION/material/skin/LOD preservation evidence.
- [ ] Identify which invariants are measured by the existing body-geometry parser and which require whole-document/component evidence. Retain all source/inspection bytes; do not claim that a body-only digest proves every pants/LOD/controller/material invariant.
- [ ] The connected-ROI movement limits are a current-method comparison surface, not automatically the historical methods' declared movement domain. Report applicability and raw differences explicitly; do not relabel global-method movement as violation of an original local contract it never used.
- [ ] RED tests: changed candidate/source/body bytes, wrong geometry selection, lost/mutable provenance, gate/profile mutation, missing component/criterion proof, historic local method relabelled target-certified, synthetic evidence promoted to real, omitted failure IDs, and identical readback falsely represented as generator determinism.
- [ ] Reuse existing parsers/oracle/gate calculations. Emit per-candidate source invariants and exact failed face/active/cohort IDs where available, plus explicitly unavailable checks. Preserve atomic whole-route membership as unresolved until exact source evidence establishes it.
- [ ] Implement deterministic readback of the same frozen bytes; no generator or solver search invocation. Separate current necessary-gate failures from claims about historical selection/search coverage.
- [ ] Emit `geometry_policy_eligibility=UNASSESSED_OR_BLOCKED` with explicit blockers; proposed NO_SAFE_GEOMETRY reason/reopening remain proposals. No event-ready result from this task.
- [ ] Run focused and existing regressions, self-review, commit; independent review before Task 2.

### Task 2: Produce the four-artifact crosswalk and adjudication evidence

**Files:** new ignored output directory, chronological report/checkpoint, no old output edits.

- [ ] Verify all candidate locations/digests against the pinned parent artifacts/source-control evidence; refuse basename or nearby-file substitution.
- [ ] Evaluate all four frozen candidates twice from unchanged code/inputs. Compare exact normalized readback reports; record original construction/selection facts separately from the current comparison results.
- [ ] Bind the observed body component and enumerate the reported pants/atomic-route membership evidence or precise missing data. A body failure can block an atomic route only after membership/atomicity are established; no separate pants outcome is inferred.
- [ ] Recover any original silhouette/nontriviality criteria and deterministic-readback evidence from the named source set. If absent, say so; do not retroactively create historical predeclarations or success/failure thresholds.
- [ ] Preserve current repeated-search evidence as accepted bounded method evidence and the defective old local repair as historical diagnostic only unless separately corrected later.
- [ ] Produce an explicit architecture/gate crosswalk and a proposed next decision: sufficient proof if independently established, or exact missing evidence / prospective architecture-policy decision. Do not automatically demand another local-method run if it need not count toward the qualifying architecture set.
- [ ] Independently review all policy claims before any terminal disposition. Canonical profile/record/protected-registry binding remains a separate mandatory gate even if geometry-policy proof later passes.
