# Bard Vanilla Landmark/Cage - Task 3 Public Checkpoint

## TL/DR

`UNFIXABLE_WITH_AVAILABLE_SAFE_TOOLING`

The declared component-specific harmonic-cage architecture exhausted the exact Vanilla pair at the fixed `0.50` completion floor. **Pants** and **Sleeves** residual failures blocked atomic admission: `0` Vanilla routes, `0` protected BCB routes, no candidate POSITION archive, no position digest.

This is offline terminal evidence for available safe automated tooling only. It is **not** an attached terminal exclusion event and does **not** authorize release completion.

### Ledger-facing bind (honest release-blocking; Plan attach not authorized)

| Field | Value |
|---|---|
| `status` | `UNFIXABLE_WITH_AVAILABLE_SAFE_TOOLING` |
| `vanilla_routes` / `replacement_routes` | `0` |
| `protected_bcb_replacement_routes` | `0` |
| `exclusion_event_input` | `null` |
| `ready_for_attachment` | `false` |
| `release_blocking` | `true` |

Do **not** mint or attach an exclusion event from this checkpoint. `exclusion_event_input` remains `null` until Plan separately authorizes attach. `ready_for_attachment` stays `false` unless that Plan gate opens.

## Outcome

The final declared component-specific harmonic-cage architecture exhausted the exact Vanilla pair at the fixed `0.50` completion floor. Pants retained `148` flipped faces and `19` faces below the `0.25` source-area floor; Sleeves retained `31` flipped faces. Atomic admission therefore emitted no candidate POSITION archive, no position digest, `0` Vanilla routes, and `0` protected BCB routes.

### Pants / Sleeves exhaustion (pair blockers at floor)

| Stream | Residual at completion floor `0.50` | Role |
|---|---|---|
| Pants | `148` flipped faces; `19` faces below `0.25` source-area floor | Exact-pair blocker |
| Sleeves | `31` flipped faces | Exact-pair blocker |
| BodyTop | passed at `0.85` | Diagnostic only; not serialized |
| Footwear | passed at `0.75` | Diagnostic only; not serialized |
| thong | passed at `0.50` | Diagnostic only; not serialized |

Partial component passes do not create a safe Vanilla pair and must not be promoted into ledger attachment inputs.

## Method and evidence boundary

- Explicit ignored component landmarks are bound to the retained named-bone common posed frame.
- Each stream uses one fixed `3 x 3 x 3` volumetric cage, exact boundary controls, a harmonic Dirichlet solve, and trilinear POSITION sampling.
- The only scalar schedule is `1.00` through `0.50` in exact `0.05` decrements.
- Ordered indices, non-POSITION semantics, skins, materials, object/component identity, and native BCB source files remain unchanged.
- Report schema: `bard_vanilla_component_landmark_cage_result_v1` (`bard_vanilla_landmark_cage_report.json`); exhausted runs emit report only (no `bard_vanilla_landmark_cage_positions.npz`).

### Evidence hashes / paths

| Artifact | Bytes | SHA-256 | Path / binding note |
|---|---:|---|---|
| Twin ignored offline Vanilla cage reports (byte-identical run-1 / run-2) | `20,646` | `45BBAAB6D63478D78D59AAAF4F44CF42D7EC710A10B3248F39CABD27E8C3EFFF` | Ignored local offline outputs; schema `bard_vanilla_component_landmark_cage_result_v1`; status `UNFIXABLE_WITH_AVAILABLE_SAFE_TOOLING`; `replacement_routes=0`; `protected_bcb_replacement_routes=0`; no POSITION candidate |
| Explicit landmark contract digest | (file digest) | `422B074B930393E3572EE6D6165A94775BD9054F94823A32C52D548F4A3311E5` | Canonical ignored path `workstreams/bard_authority_closure/local/landmarks.json` at admission time |

Tracked admission code (unchanged by this ledger bind): `workstreams/bard_authority_closure/bard_landmark_cage.py`.

This is offline terminal evidence for the declared safe automated tooling, not a mathematical-impossibility, visual-fit, animation, gameplay, package, or release claim. Reopening requires a separately approved manual-remesh, manual-sculpt/landmark, or exact replacement-source project.

## Explicit ledger boundary

- `exclusion_event_input = null` (proof/checkpoint bind only; **not** auto-attached; do not mint)
- `ready_for_attachment = false` (stays false until Plan authorizes attach)
- `release_blocking = true` (Vanilla remains release-blocking while unfixed / unattached)
- `0` Vanilla routes; `0` protected BCB routes; no safe-pair invention for Vanilla or SBBF
- Do **not** reopen geometry inventively; do **not** invent Vanilla/SBBF safe pairs from partial stream passes
- Authority Task 4 unassessable proof remains parked ignored under `local/task-4-geometry-assessment-20260906/` at SHA-256 `1C52A3F872A661442D3F91880264411224B7CE27AC2F1BF1C0D6C6284857F2CE` (28381 bytes) - **untouched** by this Vanilla ledger bind
- No PAK / live Mods / protected registry rewrite; Spike #2 not reopened; Soul Vest Alt and Padded PIN_FOLLOWUP out of slice; Grok-only edits on `grok/bard-authority-closure`

## Reopening condition

Supply a separately approved manual-remesh, manual-sculpt/landmark, or exact replacement-source project that can clear Pants and Sleeves at the declared safe tooling boundary (or a Plan-authorized successor architecture). Do not invent defect regions or attach an exclusion event from this checkpoint alone.

